name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run tests (if available)
      run: npm test || echo "No tests configured"
      continue-on-error: true
    
    - name: Build Docker image
      run: docker build -t student-management-system:latest .
    
    - name: Test Docker container
      run: |
        docker run -d -p 3000:3000 --name test-container student-management-system:latest
        sleep 10
        curl -f http://localhost:3000/health || exit 1
        docker stop test-container
        docker rm test-container
    
    # Optional: Push to Docker Hub (uncomment and configure if needed)
    # - name: Login to Docker Hub
    #   uses: docker/login-action@v2
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}
    #
    # - name: Push Docker image
    #   run: |
    #     docker tag student-management-system:latest ${{ secrets.DOCKER_USERNAME }}/student-management-system:latest
    #     docker push ${{ secrets.DOCKER_USERNAME }}/student-management-system:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Deploy to production
      run: |
        echo "Deployment would happen here"
        echo "For Render: Connect your GitHub repo and enable auto-deploy"
        echo "For AWS EC2: Use SSH to deploy or configure AWS CodeDeploy"
        echo "For DigitalOcean: Use App Platform or configure Droplet deployment"
      # Add your deployment commands here based on your hosting provider

